var titulos = 'exploro';
numero_pagina(12);

//document.getElementById("inpNotAb").addEventListener("KeyPress",validNotAbierta);

function validNotAbierta(e){
    //var key = window.Event ? e.which : e.keyCode
	//return (key >= 48 && key <= 57)
      
}
